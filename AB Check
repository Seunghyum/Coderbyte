def ABCheck(str)

  # code goes here
  word = str.split("")
  return (word.index("a") - word.index("b")).abs == 3 ? true : false 
 
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
ABCheck(STDIN.gets)           
